%h3
  = link_to 'Forum', forum_path
  = '>>'
  = link_to @topic.title, forum_subjects_path(@topic)
%br

%h4
  Create Subject

= bootstrap_form_for(@subject, url: create_subject_path(@topic)) do |f|
  = f.text_field :title
  = f.text_area :body, rows: '12'
  = f.submit 'Submit'
